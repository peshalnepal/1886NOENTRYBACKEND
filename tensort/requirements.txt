# Jetson Nano Tensort Requirements

# Core dependencies
Flask==2.3.0
numpy==1.19.5  # Python 3.6 compatible
opencv-python==4.5.5.64

# Database (async SQLite)
SQLAlchemy==2.0.23
aiosqlite==0.19.0

# TensorRT (provided by Jetson platform)
# pycuda (provided by Jetson platform or install separately)

# Optional: for better async support
greenlet==2.0.2
